# NOKOV Python SDK placeholder

Windows 采集前，把 NOKOV 厂商提供的 wheel 放在本目录并保持文件名：

```text
nokovpy-3.0.1-py3-none-any.whl
```

该 wheel 被 `.gitignore` 排除，不会上传 GitHub。获取方式、U 盘命令和已验证
SHA-256 见 [`docs/required_assets_and_downloads_zh.md`](../../docs/required_assets_and_downloads_zh.md)。
