# Local NOKOV calibration assets

本目录用于保存现场 XINGYING 相机标定、CalWand、CalFrame、场景和 Tracker 文件。

这些文件与具体场地和设备相关，默认被 `.gitignore` 排除，不上传 GitHub。请在 Linux NOKOV 工作站和离线备份盘中单独保存。

当前现场是 8 台相机。旧归档 `CalWand.vc0`～`vc5` 只有 6 台相机，不能作为当前八相机标定。必须保存当前八台相机对应的 `.cal`、完整 XINGYING 工程和资产定义。
